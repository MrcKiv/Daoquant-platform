Place your MySQL schema/data dump files here before the first `docker compose up`.

Supported init file types are the standard MySQL entrypoint ones:
- `.sql`
- `.sql.gz`
- `.sh`

This project relies on an existing `jdgp` schema and business data. The container only provides MySQL itself.
